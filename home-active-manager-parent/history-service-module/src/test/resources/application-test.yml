spring:
  application:
    name: weather-service
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    discovery:
      client:
        simple:
          instances:
            weather-service[0]:
              uri: http://localhost:8082