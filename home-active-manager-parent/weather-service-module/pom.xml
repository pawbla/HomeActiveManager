<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>home-active-manager-parent</artifactId>
    <groupId>com.pawbla.project.home</groupId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <groupId>com.pawbla.project.home</groupId>
  <artifactId>weather-service-module</artifactId>
  <version>1.0-SNAPSHOT</version>

  <name>weather-service-module</name>

  <properties>
    <release.project.folder>${project.parent.basedir}/${release.folder.name}/${project.name}</release.project.folder>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration combine.self="override">
          <archive>
            <manifest>
              <addClasspath>false</addClasspath>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <keepDependenciesWithProvidedScope>true</keepDependenciesWithProvidedScope>
              <createDependencyReducedPom>true</createDependencyReducedPom>
              <shadedArtifactAttached>true</shadedArtifactAttached>
              <transformers>
                <transformer
                        implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
                  <resource>META-INF/spring.handlers</resource>
                </transformer>
                <transformer
                        implementation="org.springframework.boot.maven.PropertiesMergingResourceTransformer">
                  <resource>META-INF/spring.factories</resource>
                </transformer>
                <transformer
                        implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
                  <resource>META-INF/spring.schemas</resource>
                </transformer>
                <transformer
                        implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
                <transformer
                        implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>com.pawbla.project.home.weather.service.WeatherServiceMain</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.coderplus.maven.plugins</groupId>
        <artifactId>copy-rename-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-file</id>
            <phase>package</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <fileSets>
                <fileSet>
                  <sourceFile>${project.basedir}/target/${project.build.finalName}.jar</sourceFile>
                  <destinationFile>${release.project.folder}/${build.finalName}.jar</destinationFile>
                </fileSet>
                <fileSet>
                  <sourceFile>${project.basedir}/scripts/start_app.sh</sourceFile>
                  <destinationFile>${release.project.folder}/start_app.sh</destinationFile>
                </fileSet>
                <fileSet>
                  <sourceFile>${project.basedir}/scripts/dev.config</sourceFile>
                  <destinationFile>${release.project.folder}/dev.config</destinationFile>
                </fileSet>
                <fileSet>
                  <sourceFile>${project.basedir}/scripts/prod.config</sourceFile>
                  <destinationFile>${release.project.folder}/prod.config</destinationFile>
                </fileSet>
              </fileSets>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
